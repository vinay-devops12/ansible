---
- name: install packages
  hosts: frontend
  become: true
  tasks:
    - name: install packages using loop
      ansible.builtin.package:
        name: "{{ item }}"
        state: present
      loop:
        - nginx
        - mysql
        - git